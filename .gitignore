# Build inputs that are not source.
tools/
.tmp-src/

# Build products (see docs/SUBAGENT_PLAN.md: payload/ and dist/ are assembled, never committed).
payload/
dist/

# Downloaded build inputs (phase 7 step 1): verified and cached inside the workspace so a
# build repeats offline. The pins for them live in build/payload-sources.json and are committed.
cache/
build/DSH_AB.exe
# 测试构建（build.ps1 -TestProduct）自己的 exe：同一个 build 目录，但绝不与正式产物同名
build/DSH_ABtest.exe
src/dsh-ab/dsh-ab.exe

# Local scratch.
.tmp-run/
.tmp-install/
.tmp-verify/
.tmp-test/
.tmp-probe/

# 本地私有：凭据（绝不提交、不进安装包、不进日志）
.secrets/

# 开发文档（PLAN/TODO/DEFECTS/EVIDENCE/VERIFY-PROTOCOL/SUBAGENT_PLAN/DOCS-DRAFT）。
# 决策 16.7：这些一律不上传。靠目录结构保证，不靠一份会漂移的文件清单：
# 公开的 docs\ 只留 MAINTAINING.md。
docs/dev/

# 记忆库与其它本地状态（绝不能上传）
.dsh-meow/
# 所有临时目录一律排除（.tmp-* 是构建/测试脚手架）
.tmp-*/
