# Changelog

## 0.1.3 — 2026-09-26（工具链重写那一轮）

- 工具链整个换成 Python：新建 `build/py/build.py`（构建五步：图标/清单资源、Go 构建、载荷、安装器编译、更新包）与 `build/py/verify.py`（验收），取代被删掉的 6 个 `build\*.ps1`（build / mkpayload / mkupdate / verify-silent / install-layout / provision-go）与 `build\verify-skill.mjs`；仓库里不再有 `.ps1`/`.bat`/`.cmd`，每条命令都走 `tools/python/python.exe`（相对路径，工作区自带），CI 里 17 处 `shell: pwsh` 也一起去掉。旧的 `build\*.ps1` 与那个 Node 脚本已从仓库删除。
- **安装器不再调用 powershell.exe，也不再调用任何外部程序**：端口探测原来 `Exec('powershell.exe')` 起一个子进程去试绑，现在在进程内用 `ws2_32` 的 `bind` 直接绑 127.0.0.1；卸载时扫描「本安装根下还在跑的 dsh」原来在 powershell 子进程里跑 `Get-CimInstance`，现在在进程内用 `kernel32` 的 Toolhelp32 走查进程表（`[Code]` 里 `Exec`/`ShellExec` 一处都不剩）。
- **`taskkill` 从停止路径里彻底消失，上一条 0.1.3 的「已知问题」到此作废**：`kill_tree = true`（出厂值）时整条进程树由本程序本来就持有的作业对象一次结束（`TerminateJobObject`），这条路上**不等待 `stop_grace_s`**——无控制台子进程收不到非强制终止请求，那段等待本来就没有可送达的对象；`kill_tree = false` 仍是单进程路，先轮询等满 `stop_grace_s`、仍活着才结束这一个进程；作业对象没接住这个子进程时也回落到单进程路（空的作业对象什么都结束不了，当成「已杀了整棵树」会把 dsh 留成孤儿占着生产端口）。
- 停止路径的两行日志文案随之变化，各自只说实际走的那条路：走作业对象或等满宽限后仍活着才结束时写「已强制结束 dsh（PID N，<原因>）」，进程自己在宽限内退出的单进程路写「已停止 dsh（PID N）」——不再两路同一句话。`launch.stop_grace_s` 的注释同步改成现在的口径（只对单进程路生效）。

## 0.1.3 — 2026-09-25
- 托盘看得见「别人写进去的」槽位切换登记：每 2 秒重读 state\（以前只在启动时读一次）。此前由维护脚本直接写的 pending.json 在菜单和状态弹窗里都看不见，而且点「重启」时读的是内存里的旧值，切槽会**静默不发生**
- 状态弹窗改成逐槽报**各自的** dsh 版本（读槽内 node_modules\@deepseek-ai\dsh\package.json）与会话数，安装行只留自身版本，不再打印编译时随包的那个 dsh 版本。这是 DSH-AB「从不解析 dsh 内容」的第一处只读例外，只读一个 version 字段
- 日志三档口径修正：auto 现在包含 info（此前 auto 写 warn/debug 却不写 info —— 干净启动连日志文件都不建，档位形同虚设）；off 只记 error；full 再加 debug 与子进程输出。托盘改档那一行也从 debug 提为 info，保持「auto 档也看得见改档」的原有口径
- **强杀托盘会连带关闭 dsh**：dsh 子进程被放进一个带 JOB_OBJECT_LIMIT_KILL_ON_JOB_CLOSE 的 Windows 作业对象，托盘无论以何种方式消失（任务管理器强杀、崩溃）都会把 dsh 一起带走，不再留下占着生产端口的孤儿进程；拿不到作业对象时只记一条 WARN 并照常运行。正常退出的代码路径未改动（但见文末已知问题：它并不真的优雅）
- --help / -h 打印动词表后退出（以前会静默启动一整个托盘）；**任何不认识的动词**（旧文档里已被删除的 --slot-guard、拼错的开关）现在都打印同一份用法、以 2 退出，不再静默启动第二个托盘与一个 dsh
- 「发现外部登记的槽位切换」的提示改成**每次登记各出一次**（登记被清掉时把闩打开），不再一进程只出一次；同一进程里第二次登记过去只有菜单和状态弹窗更新，提示与日志行都不出现
- 重启确认框只在**真有**待生效登记时才说「已经登记的槽位切换会生效」，并在有登记时一并报出两个槽各自的会话数

已知问题（既存行为，本轮只把它说清楚，没有发明「优雅停止」）：出厂 `kill_tree = true` 时 `Stop()` 先跑的 `taskkill /PID N /T`（不带 `/F`）对控制台子进程必然失败（exit 128），于是立刻转为硬杀、**从不等待 `stop_grace_s`**，该配置项因此形同虚设——这条现在如实写进了 `launch.stop_grace_s` 的注释，日志也按实际走的那条路分别写「已停止」与「已强制结束，未等待 stop_grace_s」，不再两路同一句话。

## 0.1.2 — 2026-09-23
- 就地更新：Release 里多一份 `DSH-AB-<版本>-update.zip`（安装根里除两个槽以外的程序文件，一个 DSH-AB 版本一份），覆盖上去即完成更新，dsh 与托盘不用重启；`dsh-ab.toml` 与 `state\` 原样保留，回退靠更新前的那次 git 快照
- 启动时把「应用和功能」里的版本与大小对齐到本程序自己的版本与安装根实际体积（就地更新不经过安装程序，这两项原本会停在旧值）
- dsh 0.1.5-alpha.2 能构建了：载荷内的相对路径由构建期门禁卡在 180 字符以内（超了就直接报出那条路径），ISCC 读得动整棵载荷
- dsh 0.1.5-alpha.1 能构建了：`build\verify-skill.mjs` 不再写死 dsh-skill 的入口位置，按候选位置查找
- Release 说明最上方加一张「最新在前」的下载表：asset 列表按文件名排序，最新版会被排到下面去
- 卸载选「保留用户数据」（默认）时不再删掉随包那份 skill：它在 `slot-a\data` 里，而那条 [Files] 现在带 `uninsneveruninstall`，用户数据真的原样保留
- 安装包不再静默漏装文件：安装器的 [Files] 排除项少了开头的反斜杠，匹配到了任意层级同名的项，45 个文件（依赖自带的数据目录）一直没进安装根；现在安装根里的每个文件都要与构建期记录逐个对上，少一个或多一个都直接报出来
- 更新包不再带 `docs\`：就地覆盖安装根时不会再把这份安装累计的台账顶回模板那一份
- 静默卸载多了 `/DELETEUSERDATA=1`：以前只有「保留用户数据」这一条路可选（默认仍是保留），现在「删」那一路也能由脚本走通，并且真机验过
- 安装包与更新包可以字节复现：源文件时间戳不再进产物，同一份源码连编两次得到同样的字节

## 0.1.1 — 2026-09-22
- 默认生产端口改回 3090，安装向导、配置文件与程序内置默认值三处一致
- 名字算成别人、快捷方式认不出来两处缺陷修好；开始菜单不再用子文件夹，快捷方式永远直接放在 `开始菜单\程序\<名字>.lnk`
- 启动前先查生产端口：被占用就直接报错，不写「已就绪」、不开浏览器、不建子进程
- 端口被占时只留一条「dsh 启动失败」弹窗，命名提示不再跟着一起弹
- 卸载器三处修复：PowerShell 5.1 下语法错误导致一个进程都没结束、命令行匹配会误杀别的安装的进程、改名过的快捷方式漏删
- 日志三档口径改为 off 只记 error、auto 再加 warn/debug、full 全记（含带槽位标记的子进程输出），托盘改档在 auto 档也看得见
- 按过度工程审计精简 8 条（约 -412 行，0 新增依赖）
- 装进安装根的文档去掉硬编码，随包 skill 不再指路端口与要复制的目录

## 0.1.0 — 2026-09-20
首个版本。

- 单个 Windows 安装程序，内置 Node、dsh 应用树、npm 依赖闭包与 MinGit，装后可离线运行
- 两个完整槽位；改动只做在非活动槽，在空闲端口上验证后再登记切换
- 托盘六项固定菜单：打开浏览器 / 当前状态 / log / 槽位切换 / 重启 / 退出
- 全中文安装向导与原生 Windows 对话框；卸载保留用户数据，可选择性删除
- 安装程序内置并显示所带的 dsh 版本（文件名同样标注）
